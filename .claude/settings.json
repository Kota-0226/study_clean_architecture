{
    "permissions": {
      "deny": ["Read(./1st_try/**)"]
    },
    "hooks": {
      "PreToolUse": [{
        "matcher": "Bash",
        "hooks": [{
          "type": "command",
          "command": "jq -r '.tool_input.command' | grep -q '1st_try' && { echo '1st_try の参照は禁止です' >&2; exit 2; } || exit 0"
        }]
      }]
    }
}